// ============================================================
// utils/regex.ts — 正则转义工具
// ============================================================

/**
 * 对字符串中的正则特殊字符进行转义，使其在正则表达式中被当作字面量匹配。
 */
export function escapeRegex(str: string): string {
  return str.replace(/[.*+?^${}()|[\]\\]/g, '\\$&');
}
